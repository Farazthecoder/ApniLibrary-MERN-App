# Backend Setup


```npm install```

This will install all the dependencies for the project.

## Setup the database

1 create .env file in the root directory and add the following

```MongoDB_URI=mongodb://localhost:27017/<db_name>```

```PORT=<port_number>```

## START THE SERVER

```npm run start```

This will start the server and listen on the port specified in the .env file.

***
***

# Vite FrontendSetup

1 Install all the dependencies by running

```npm install```

2 change the url assigned of the post and the get request in the component which is used to fetch the data from the backend server.

3 After the changes are made, run the project by running

```npm run dev```





